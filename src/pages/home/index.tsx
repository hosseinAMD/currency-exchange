import IconButton from "components/common/icon-button";

const Home: React.FC = () => {
  return (
    <div>
      <p>Convertor</p>
      <IconButton icon="compare_arrows" onClick={() => {}} />
    </div>
  );
};

export default Home;
